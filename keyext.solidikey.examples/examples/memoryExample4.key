\include "./commonFields.key";

/*
    alice = new Person();
    return alice.age;
*/

\rules {
    aliceDef {
        \find(alice)
        \replacewith(idC(ca, mt))
        \heuristics(concrete)
    };

    defaultAge {
        \find(defaultF(age))
        \replacewith(0)
        \heuristics(concrete)
    };
}

\problem {
    { mem := addM(mem, ca) }
        { v := int::read(mem, alice, age) }
            (v = 0)
}
