\functions {

 \unique Field balance;
 Struct s;
 List xs;
 any x;

}

\include "./commonRules.key";

\problem {
    // int::head(cons(0,mt)) = 0
    // tail(cons(0,mt)) = mt
    // int::last(cons(0,mt)) = 0
    // int::last(cons(1, cons(0,mt))) = 0
    // firsts(cons(1, cons(0,mt))) = cons(0, mt)
    true
}