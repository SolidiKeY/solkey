\programSource "../TestSuite.sol";

\problem {
    \<{
        tokens.push(Token(42));
        tokens.pop();
        tokens.push();
        assert(tokens.length == 1);
        assert(tokens[0].value == 0);
    }\> true
}
