\programSource "PaperTest.sol";

\problem {
    \<{
        alice.account.balance = 10;
        alice.account.token.value = 5;
        int b = alice.account.balance;
        uint v = alice.account.token.value;
        assert(b == 10);
        assert(v == 5);
    }\> true
}
