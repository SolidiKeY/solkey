\programSource "PaperTest.sol";

\problem {
    find<[int]>(storage, cons2(PaperTest$people, size)) = 0 ->
    \<{
        Account storage acc = people.push().account;
        acc.balance = 50;
        assert(people.length == 1);
        assert(acc.balance == 50);
    }\> true
}
