\functions {
    IdentityPrim p;
    Identity id;
    Memory m;
    Struct s;
    Struct t;
    \unique MemberField f;
    \unique MemberField g;
    \unique MapMemberField mf;
}

\problem {
    selectSt<[Struct]>(copyMem(s, m, id), f) = copyMem(mtSt, m, read<[Identity]>(m, id, f))
}
