\functions {
    IdentityPrim p;
    Identity id;
    Memory m;
    Struct s;
    Struct t;
    \unique Field f;
    \unique Field g;
    \unique MapField mf;
}

\problem {
    find<[int]>(copyMem(s, m, id), cons(f, nil)) = read<[int]>(m, id, f)
}
