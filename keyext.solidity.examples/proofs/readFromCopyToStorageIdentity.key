\functions {
    IdentityPrim p;
    Identity id;
    Memory m;
    Struct s;
    Struct t;
    \unique Field f;
    \unique Field g;
    \unique MapField mf;
}

\problem {
    read<[Identity]>(copySt(m, p, s), idC(p, nil), f) = idC(p, cons(f, nil))
}
