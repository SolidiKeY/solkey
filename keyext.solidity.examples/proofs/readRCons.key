\functions {
    IdentityPrim p;
    Identity id;
    Memory m;
    \unique MemberField f;
    \unique MemberField g;
}

\problem {
    readR<[int]>(m, id, cons(f, cons(g, nil))) = read<[int]>(m, read<[Identity]>(m, id, f), g)
}
