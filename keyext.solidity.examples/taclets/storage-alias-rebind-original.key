\programSource "PaperStore.sol";

\programVariables {
    int before;
    int result;
}

\problem {
    \<{ before = alice.age;
        Person storage alicePath = alice;
        bob.age = 20;
        alicePath = bob;
        result = alice.age; }\>(result = before)
}
