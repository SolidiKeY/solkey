\programSource "PaperStore.sol";

\include "./storageRules.key";

\programVariables {
    int result;
}

\problem {
    (\<{ st.age = 34; }\>
        (find<[int]>(storage, cons1(age)) = 34))
    &
    (\<{ result = st.age; }\>
        (result = find<[int]>(st, cons1(age))))
}
