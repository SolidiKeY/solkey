\programSource "PaperStore.sol";

\programVariables {
    int result;
}

\rules {
    defaultPaperStoreAge {
        \find(defaultF(PaperStore$age))
        \replacewith((any) 0)
        \heuristics(concrete)
    };
}

\problem {
    \<{ age = 10; delete age; result = age; }\>(result = 0)
}
