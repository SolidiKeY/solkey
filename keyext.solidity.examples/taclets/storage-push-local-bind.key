\programSource "PaperStore.sol";

\problem {
    find<[int]>(storage, cons2(PaperStore$persons, size)) = 2 ->
        \<{ Person storage p; p = persons.push(); }\>
            (find<[int]>(storage, cons2(PaperStore$persons, size)) = 3)
}
