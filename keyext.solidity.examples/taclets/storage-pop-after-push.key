\programSource "PaperStore.sol";

\problem {
    find<[int]>(storage, cons2(PaperStore$values, size)) = 0
    & !(at(0) = size) ->
        \<{ values.push(); values.pop(); }\>
            (find<[int]>(storage, cons2(PaperStore$values, size)) = 0)
}
