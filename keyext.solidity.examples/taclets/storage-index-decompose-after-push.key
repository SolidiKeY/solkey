\programSource "../TestSuite.sol";

\programVariables {
    int result;
}

\problem {
    0 < find<[int]>(storage, cons2(TestSuite$matrix, size))
    & find<[int]>(storage, cons3(TestSuite$matrix, at(0), size)) = 0 ->
        \<{ result = storageIndexDecomposeAfterPush()@TestSuite; }\>(result = 7)
}
