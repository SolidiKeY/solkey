\programSource "PaperStore.sol";

\problem {
    find<[int]>(storage, cons2(PaperStore$values, size)) = 2 ->
        \<{ values.push(); }\>
            (find<[int]>(storage, cons2(PaperStore$values, size)) = 3)
}
