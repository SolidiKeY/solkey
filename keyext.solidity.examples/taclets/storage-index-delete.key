\programSource "PaperStore.sol";

\programVariables {
    int result;
}

\problem {
    1 < find<[int]>(storage, cons2(PaperStore$values, size)) ->
        \<{ values[1] = 7; delete values[1]; result = values[1]; }\>(result = 0)
}
