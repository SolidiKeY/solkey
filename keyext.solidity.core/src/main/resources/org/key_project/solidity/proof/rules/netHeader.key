\programVariables {
   Struct net;
   int msgSender;
   int msgValue;
   int self;        // address of the contract under verification
   int selfBalance; // funds held by the contract under verification
}

\predicates {
   CInv(Struct, Struct);   // contract invariant over (storage, net)
}
