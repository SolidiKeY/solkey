// State-variable access inside a diamond: `value` is a purely syntactic field reference in
// the program AST (no program variable). fieldWriteThenRead moves the write into a storeSt
// update and reads it back; the existing struct rule selectIntOnStore closes the goal.

\programSource "FieldBox.sol";

\schemaVariables {
    \modalOperator {diamond, box} #mod;
    \program FieldReference fr;
    \program SimpleExpression se;
    \formula post;
}

\rules {
    fieldWriteThenRead {
        \schemaVar \program FieldReference fr;
        \schemaVar \program SimpleExpression se;
        \schemaVar \formula post;
        \find(\modality{#mod}{ s#fr = s#se; }\endmodality(post))
        \replacewith({storage := storeSt(storage, fr, se)} selectSt<[int]>(storage, fr) = se)
        \heuristics(simplify_prog)
    };
}

\problem {
    \<{ value = 42; }\>(true)
}
