\functions {

 \unique Field balance;
 Struct s;
 List xs;
 any x;

}


\problem {
    // head<[int]>(cons(0,nil)) = 0
    // tail(cons(0,nil)) = nil
    // last<[int]>(cons(0,nil)) = 0
    // last<[int]>(cons(1, cons(0,nil))) = 0
    // firsts(cons(1, cons(0,nil))) = cons(0, nil)
    true
}
