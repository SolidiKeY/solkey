\include "./commonFields.key";

\problem {
    find<[int]>(
        save(
            save(mtSt, cons2(account, age), 20),
            cons2(account, balance),
            30
        ),
        cons2(account, age)
    ) = 20
}
