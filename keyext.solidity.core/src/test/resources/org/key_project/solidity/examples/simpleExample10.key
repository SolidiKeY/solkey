\include "./commonFields.key";

\problem {
    find<[int]>(
        save(
            mtSt,
            cons1(balance),
            read<[int]>(write(mem, bob, age, 20), bob, age)
        ),
        cons1(balance)
    ) = 20
}
