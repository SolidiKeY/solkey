\include "./commonFields.key";

\schemaVariables {
    \modalOperator {diamond, box} #mod;
}

\rules {
    skipVarExpr {
        \schemaVar \formula post;
        \schemaVar \program Variable pv;
        \find(\modality{#mod}{ s#pv; }\endmodality(post))
        \replacewith(post)
        \heuristics(simplify_prog)
    };
}

\problem {
    \<{ u; }\>(true)
}
